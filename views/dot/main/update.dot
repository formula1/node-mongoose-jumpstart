{{
var url = it.utils.object2URL(it.mvc.instance);
}}
<h1>{{=it.mvc.model.modelName}}</h1>
<form
  id="{{=it.utils.getUnique()}}"
  method="POST"
  action="{{=url}}/!/save"
  enctype="multipart/form-data"
>
{{
  var nestedPaths = {};
  var model = it.mvc.model;

  for(var i in model.schema.paths){
}}
  {{
    if(i.match(/^_|_$/))
      continue;
    var currentpath = model.schema.paths[i];
  }}
  {{
      currentpath = model.schema.paths[i];
  }}
  <div>
    <strong
      {{ if(model.schema.paths[i].isRequired){ }}
      style="color:#F00"
      {{ } }}
    >{{=currentpath.path}}:</strong>{{=it.mongooseUI(currentpath, it.mvc.instance, true)}}
  </div>
{{  }  }}
  <div>
    <button type="submit">Save</button>
    <a href="{{=url}}"><button type="button">Cancel</button></a>
  </div>
</form>
