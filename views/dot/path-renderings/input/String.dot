{{
  if(typeof it.value == "undefined")
    it.value = "";
  if(it.path.hasOwnProperty("enumValues") && it.path.enumValues.length > 0){ }}
  <select name="{{=it.path.path}}">
    {{ for(var j=0;j<it.path.enumValues.length;j++){
        if(it.value == it.path.enumValues[j]){ }}
        <option selected="selected" value="{{=path.enumValues[j]}}" >
          {{=it.path.enumValues[j]}}
        </option>
      {{ }else{ }}
        <option value="{{=it.path.enumValues[j]}}">
          {{=it.path.enumValues[j]}}
        </option>
      {{
        }
      }
    }}
  </select>
{{ }else if(it.path.regExp != null){ }}
  <input type="text" name="{{=it.path.path}}" value="{{=it.value}}" pattern="{{=it.path.regExp}}" />
{{ }else{ }}
  <input type="text" name="{{=it.path.path}}" value="{{=it.value}}" />
{{ } }}
