<div>
<h3>{{=it.path.path}}</h3>
{{ for(var i in it.path.subpaths){
}}
{{=it.mongooseUI(
      it.path.subpaths[i],
      it.mpath.get(it.path.subpaths[i].path,it.value)
    )}}
{{
}
}}
</div>
